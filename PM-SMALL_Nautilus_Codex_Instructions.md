# PM-SMALL 迁移至 NautilusTrader：Mac Codex 完整执行指令

你现在接手一个完整的交易程序迁移任务。请阅读本文件，检查代码，先形成完整设计，再持续完成实现、必要测试及交付准备。不要只给建议、目录骨架或 Implementation Plan 就结束，也不要让我重复确认本文件中已经明确的产品规则。

## 一、项目来源与本次目标

原项目是我自己编写的 [PM-SMALL](https://github.com/yvettemiranda/PM-SMALL)。新程序使用 [NautilusTrader](https://github.com/nautechsystems/nautilus_trader) 作为交易底座。

我的目标是：保留 PM-SMALL 的完整交易规则、现有 UI 和设置方式，将交易基础设施迁移到 NautilusTrader；新程序从空仓和全新账本开始，先使用真实行情进行模拟交易，经过验证后，再使用同一套策略规则进行实盘。最终程序长期部署在我的服务器上，源代码保存在我自己的 GitHub。

2026-09-06 核对到的 PM-SMALL `main` 提交为：

`eb8c6d8a09f9b0427890b7a2d744fc3485d1d3dc`

开始时重新核对远端，记录实际采用的完整 SHA。如果远端已有更新，先比较更新内容，将影响迁移的差异写入交接记录，不要悄悄混用不同版本的规则。

优先阅读原项目 `HANDOFF.md`、`docs/DECISIONS.md`、`README.md`、`docs/PLAN.md`、`docs/SERVER_DEPLOY.md`、`SECURITY.md`，再检查相关实现和测试。重点入口包括：

- `src/domain/` 中的筛选、仲裁、FAK、目标价、止损和结算逻辑。
- `src/services/` 中的自动交易、行情处理、配置和结算服务。
- `src/infrastructure/db/database.ts` 中实际执行的周期、资金和持久化规则。
- `src/web/index.html`、`src/web/app.js`、`src/web/styles.css` 及 `src/app.ts` 中的 UI/API 契约。

本文件是本次迁移的新要求。原项目中的“当前只交付 TEST”“不实现钱包或 LIVE”等旧阶段限制，不应阻止本次编写完整实盘接入及自动赎回功能；实际运行仍遵守下文的 TEST 交付边界。旧文档中的历史发布授权不适用于本次服务器操作。已取消的 Core/Runner、回本规则、OBSERVE 和其他历史方案不带入新项目。

## 二、已经确定的范围

1. 保留现有 UI 的布局、主要字段、手机使用方式、设置入口和 START/PAUSE 操作。仅为接入新后端及显示真实资金、赎回状态做必要修改，不重新设计页面。
2. 交易参数继续通过 UI 设置。TEST 使用可配置的模拟初始资金；LIVE 的资金依据实际交易账户或钱包中可用于下单的余额，不能填写一个虚拟初始资金替代真实余额。
3. 每个 Event 每轮投入金额继续由 UI 控制。盈利、卖出或赎回到账后，可用资金继续参加后续交易，每轮金额不会自动放大。
4. 只管理本程序产生的订单和仓位。不迁移旧仓位、旧订单、旧账本，不接管手动买入的仓位，不开发历史数据兼容工程。
5. TEST 与 LIVE 使用同一套筛选、仲裁、预算、买入、目标卖出、止损和资金循环规则。差异放在模拟执行、真实执行及回款接入处。
6. 自动赎回属于完整交付范围。未来 LIVE 必须形成“正式结算 → 自动赎回 → 确认资金到账 → 更新可用余额 → 按原规则继续交易”的闭环。
7. 部署目标是我的 Linux 服务器，手机和电脑通过原有风格的网页操作。Mac 用于开发、检查和部署，长期交易进程在服务器上运行。
8. 默认交付状态为 `TEST + PAUSED`，实盘执行未启用。本次应写完能够接入真实账户的功能，但不实际导入我的钱包凭据、不签署真实订单、不下实盘单、不执行 approve、redeem 或其他链上写操作。正式长期 TEST 由我在整体交付后启动；LIVE 由我完成测试验收和账户配置后明确启用。

上述规则已经确定。不要再询问是否保留 UI、是否需要自动赎回、是否接管手动仓位、是否迁移旧账本、是否部署自己的服务器。

## 三、技术实现原则

先做最小版本兼容检查，选定可安装、可运行且满足本项目 Polymarket 需求的 NautilusTrader 版本，再完成整体设计。

截至 2026-09-06，GitHub latest release 指向 [v1.231.0 Beta](https://github.com/nautechsystems/nautilus_trader/releases/tag/v1.231.0)，发布说明处于 v1/v2 切换阶段；[latest Polymarket 文档](https://nautilustrader.io/docs/latest/integrations/polymarket/) 描述了 Rust 适配器和预发布安装方式。这些仅是核查起点，执行时以实际版本为准。不要安装一个版本，却直接照另一版本的文档和示例编写。

记录所选版本、必要时的源码 SHA、Python 版本、Mac 和目标 Linux 的架构要求，提交依赖锁文件。固定可复现版本，不在每次启动时自动追踪 latest 或 develop。框架版本选择由你根据兼容性证据完成；若必须采用预发布版本，说明原因和未验证部分。

采用独立的新应用项目，优先通过依赖使用 NautilusTrader，使用 Python 编写策略和接入代码；不整体复制 NautilusTrader 源码，也不先修改框架核心。确需上游补丁时，保持最小范围并记录版本和原因。

应真正使用 Nautilus 的行情、策略事件、订单、成交、账户、持仓及执行核对能力。现有前端接到新应用 API。应用自己的持久化只补充 Event 周期、冻结预算、分批目标、止损观察、配置、交易归属和赎回跟踪等业务状态；框架已提供的交易基础能力应直接利用。[执行机制参考](https://nautilustrader.io/docs/latest/concepts/execution/)

以最少服务和依赖完成任务。不要继续运行旧交易引擎再外包一层 Nautilus，不保留两套相互独立的交易事实账本，也不要自行搭建通用多账户平台、插件系统、复杂消息中间件或大批防御性抽象。必要的资金不超额、成交不重复、Event 不冲突和赎回不重复属于正确执行要求，应结合现成机制简洁实现。

## 四、需要完整迁移的规则

以下是已核对的核心规则。其他参数合法范围、字段及交互细节从采用的 PM-SMALL 代码和有效决策中补齐，形成规则对应表，不能仅照本节摘要删减功能。原代码中已证实的错误应修正并记录。

### 1. 扫描与市场资格

- 遍历全部开放 Event 的全部分页，不增加隐藏的 Event、Market、Token 数量上限，也不使用事件级日期窗口提前截断。
- Event 和具体 Market 必须明确开放。Market 还须允许下单、启用订单簿，且具有有效 Condition 身份。
- 单 Market Event 作为二元；标准 neg-risk 多 Market Event 按完整结果数分类：二元、三元、多元（4+）。默认开启二元和三元，多元关闭；排除 `negRiskAugmented`。子 Market 关闭后不缩小 Event 的原始结果数。
- 栏目按 Polymarket 官方首页 Tag 同步，默认全选；不增加历史成交量硬门槛。
- 总时长采用具体 Market 的开始至结束时间，缺失时按原规则回退 Event 时间。默认 1–30 天，最短和最长各允许 1–365 个完整天数。不得用剩余时间代替总时长。
- 市场已经开始、尚未结束，生命周期进度默认不超过 20%，可配置 1%–100%。体育比赛开赛后不再新买，已有仓位继续退出或结算。
- 静态合格 Token 都进入行情监控，包括当前超出买价范围或暂时无可成交盘口的 Token。盘口变化只重算相关 Event，不逐笔遍历全部市场。

### 2. 盘口、预演与 Event 仲裁

- 买价为闭区间，最低和最高均按 0.1¢ 调整，范围 0.1¢–99¢，默认也是 0.1¢–99¢。不能恢复为旧版 1–3¢。
- 需要完整可执行盘口、有效 Best Bid、合法 tick、最小下单量和明确费用信息。Best Bid / Best Ask 默认至少 50%，可配置 1%–100%。
- 未锁定 Event 必须等全部静态合格兄弟 Token 的盘口完整后才仲裁。明确收到的空盘口只淘汰对应 Token；尚未收到、重连或未知的盘口会阻断该 Event 新开仓。Event 已锁定后，兄弟盘口问题不阻断 active Token 的正常退出。
- FAK Preview 无副作用，带入当前盘口、费用、资金、每轮预算、tick、最小量和已消费深度，计算实际可买份额与目标。Preview 不产生订单、锁、成交或资金变化。
- 同一 Event 的比较顺序为：Best Bid / Terminal Target、按共享买盘计算的退出覆盖率、预计花费 / 本轮预算、Best Bid / Best Ask、扣除买卖费用后的目标净收益率、生命周期配置方向、稳定的 Market/方向/Token ID。较高数值优先；生命周期和 ID 按原实现的确定性方向处理。Terminal Target 为该 Preview 各笔目标价中的最高值。
- 不使用人工加权总分，无成交机会不能成为 Winner。跨 Event 保持 READY 优先，组内按配置的生命周期方向处理，默认正序。
- 发单前重新核对最新配置、盘口、余额和 Event 状态；旧 Winner 失效时重新仲裁。

### 3. 买入与周期预算

- 按实时 Ask 触发 FAK，在价格上限内从低到高成交，允许部分成交，未成交余量取消，不改成等待成交的 Maker 策略。
- 默认模拟初始资金 100U，每个 Event 每轮 1U。首笔实际非零成交确认 active Token，并冻结本轮金额；尚未发生任何卖出、仍有预算且仍满足新买资格时，可继续买入同一 Token。
- 同一轮只允许一个 active Token，所有兄弟 Token 不能同时开仓。轮中增加 UI 金额不能扩大旧周期预算。
- 无成交不能建立正式持仓周期；实盘发单等待期间所需的临时占用，与首笔成交后成立的正式周期要区分清楚。
- 预算、资金、订单及业务状态应保持一致，但不能把本地 SQLite 事务当作能够覆盖远端成交的事务。真实周期变化以框架接收的成交事实推进。

### 4. 目标卖出与重新开轮

每笔实际买入 Fill 的目标价：

`min(99¢, 按市场 tick 向上取整(max(实际买入成交价 + A, 实际买入成交价 × B)))`

- 默认 A=1¢、B=1.5。目标按各次实际成交生成；后续修改 A/B 不重写已有目标。
- Best Bid 达到目标后按 FAK 从高到低卖出，允许部分成交，剩余继续等待原目标。
- 低目标优先，同目标按创建时间和稳定 ID 排序。小额目标可按原规则聚合到最小下单量，批次限价取所含目标中的最高目标；成交再分配回各目标。
- 多目标共享同一份可消费 Bid 深度，不可重复使用同档流动性。Preview 和 TEST 实际卖出对批次、费用及深度的处理应一致。
- 买入产生目标后立即检查当前盘口，不要求等下一条行情。
- 本轮首次实际卖出后，整个 Event 停止新买。清仓且无活动卖单后释放 Event，仍合格时自动开始下一轮仲裁，不增加手动开轮操作。

### 5. 主动止损

- 默认开启，M=0.40，合法范围为 0<M<1。开关及倍数在本轮首笔实际成交时冻结，不受后续配置追溯影响。
- 基准为本轮实际买入 Fill 按扣费前成交份额加权的均价，止损线为均价×M，沿用整数微单位计算口径。允许加仓时的新 Fill 会更新基准并重新开始观察。
- 完整可执行 Best Bid 严格低于止损线，且至少两个不同的有效 Bid 盘口版本之间相隔不少于 30 秒，才完成触发确认。等于或高于阈值重置观察；空 Bid、未知、重复版本、时间倒退不推进确认。
- 从 ARMED 开始不再新买；触发后进入不可逆 EXITING，取消普通目标和活动买单，按 FAK 主动退出。部分成交继续退出，价格恢复也不恢复普通目标。
- 普通目标仅部分卖出时，剩余仓位继续受止损保护。最终清仓或触发后的正式结算使止损进入完成状态。
- 止损观察及禁止重入状态需持久化。触发后的 Event 在本次 TEST 内不重入，直到正式重置；LIVE 的对应禁入记录同样不能因暂停、重连或重启而解除，且不得用 TEST 重置清除真实记录。

### 6. 费用、估值与配置

- 保留明确的费用启用状态及动态费率/指数校验。买入费扣所得份额，卖出费扣到账金额，盈亏按净额计算；实盘以平台实际成交及费用回报为准，处理时避免重复扣费。
- 保留现有 Best Bid 扣预计卖出费用的持仓估值口径；明确无 Bid 时估值为 0，数据未知时显示未知。不要擅自改成全深度清算估值或成本价估值。
- 总资金区保留总资金、已实现、未实现、持仓价值和单数。LIVE 若存在待赎回金额，应明确归类，避免将应收资金计为可下单现金或重复计入总资金。
- 保存配置影响后续新买、新 Fill 目标和新周期冻结值，已冻结的目标和周期规则保持原值。失去资格的活动买单应撤销未成交部分，已成交部分继续管理。

### 7. START、PAUSE、重置与模式

- START 继续当前账本，不清空资金、目标、仓位和历史。
- PAUSE 停止新买并撤销买单未成交部分；已有目标卖出、止损、结算和自动赎回继续运行。
- TEST 重置仅在 PAUSED 下进行，保留现有双重确认，清空本次 TEST 并恢复全部原定默认值。模拟初始资金只能在暂停且无交易历史时直接修改。
- 重置后的新 TEST 不接受旧会话在途行情、结算等回调写入。不要把旧项目 Schema 迁移和旧账本导入带过来。
- TEST 与 LIVE 的账户、订单、持久化状态和记录分别存放，业务代码共用。UI 切换视图不自动转移仓位、复制模拟资金或启动另一模式的交易。

### 8. 正式结算与自动赎回

- 按具体 Condition 处理，要求正式关闭及官方 resolved/settled 结果。保留 1/0 与官方 0.5/0.5 结果支持，结果不明确时等待，不猜测。
- TEST 模拟结算和回款；LIVE 完成真实自动赎回。已结算 Condition 不再开仓，仍开放的兄弟 Condition 可在满足解锁规则后继续参与。
- 不把“收到结算事件”“提交赎回交易”和“资金已到账”当作同一件事。结算识别后继续保存未赎回权利，即使框架已关闭该 instrument 的持仓视图，也不能丢失赎回跟踪。
- 按实际钱包类型、当前抵押资产和标准/neg-risk 市场选择正确的官方赎回路径，核对签名账户、资金账户、合约、必要授权和手续费来源。只实现项目实际所需路径，不泛化成钱包平台。
- 赎回需处理成功、等待、失败及重启后的继续查询，防止重复赎回和重复记账。资金确认成为实际可用余额后才重新用于交易；暂停时可以回款，但不得因此新买。
- Nautilus 的市场关闭事件不自动完成链上赎回；当前文档也不能证明 0.5/0.5 处理与原规则完全一致，应按所选版本补齐并验证。[市场结算事件说明](https://nautilustrader.io/docs/latest/integrations/polymarket/#market-resolution-events)

## 五、UI 与实盘接入的验收要求

UI 保持手机和桌面共用的紧凑单列布局，至少支持 320px；保留配置、五项资金摘要、当前持仓、默认折叠的交易记录和按 Event 聚合的扫描列表。保留前 20 项展开、生命周期排序、分批成交归组和约 500ms 的轻量刷新，不让刷新覆盖未保存设置。

优先兼容现有 API 契约，确需调整时同步前后端，逐项核对参数实际生效。不能出现“页面有设置，策略没有读取”的情况。工程验证详情写入文档或诊断接口，用户页面只呈现操作所需信息。

LIVE 设置保留每轮金额等策略参数，实际余额只读。认证、凭据和运行配置放在服务器端，浏览器不保存或接收钱包私钥。TEST 启动不依赖钱包，也不建立私有交易连接。

实盘实现至少应闭合以下关系：

- 发单至回报期间占用本次现金额度并保持 Event 临时互斥；拒单或确认取消后正确释放，部分成交转入正式周期。
- 实际订单量、FAK 类型、价格边界、金额/份额换算、tick、精度、最小下单量及费用符合所选适配器和 Polymarket 当前要求。不自动提高用户预算以凑最小金额。
- 迟到成交、取消与成交交错、订单结果待确认时，真实成交仍只入账一次，不能因撤单请求已发出就当作已撤销。
- 重启后利用框架机制核对本程序订单及持仓，再恢复自定义周期、目标、止损和待赎回状态；不扩大为接管全部钱包历史仓位。
- 框架事件与应用业务持久化之间具有可验证的恢复关系，不能因为进程中断丢失某笔 Fill 对目标、预算或赎回的影响。

TEST 优先采用所选版本的 Nautilus 模拟执行能力。检查其默认撮合是否符合原 FAK、部分成交、共享深度和净份额费用规则；确有差异时通过兼容的最小扩展补齐，使订单及成交仍走 Nautilus 的执行事件。LIVE 始终依据真实回报，不能把 Preview 结果直接记为真实成交。

## 六、迁移时需要复核的已有问题

前次审查在上述 PM-SMALL 提交中复现了两项模拟深度问题，请先在采用版本中复核，再避免带入新实现：

1. `paper-market-processor.ts` 按整侧盘口生成版本，数据库按该版本记消费。较低 Bid 档位发生变化时，可能释放已经消费的更高 Bid 档位，造成重复模拟卖出。复现场景：10 份仓位，3.5¢ 档只有 5 份深度，先卖 5 份；仅改变下方 1¢ 档数量，不应再次获得原 3.5¢ 档的 5 份可用深度。明确模拟补充深度的规则，不能把任何无关档位变化都视为全部流动性重置。
2. `getOrderBook()` 提供的 `bookVersion` 来自 Ask 版本，`planTestFakBuy()` 的退出预演却用它查询 Bid 消费；实际卖出采用 Bid 版本，可能让 Preview 重复计算已经消耗的买盘。两侧版本与消费记录需一致。

修正这些错误时记录预期结果，不能以与旧错误结果完全相同作为迁移成功标准。

前次本地类型检查和构建通过，但 324 项测试中有 8 项受 tsx IPC 权限限制未通过；远端 Linux 曾有一个短超时 CLI 用例失败。接手后按当前环境核对，区分环境限制、原有问题和迁移回归，不宣称历史测试全部通过，也不为通过测试删除有效断言。

没有实际证据时，不增加任意行情超时阈值、大量重试层或通用兜底框架；安静盘口不因没有价格变化自动成为错误盘口。

## 七、执行顺序与验证节奏

1. 检查本地工作区和适用的 AGENTS.md，保护已有未提交修改。建立独立新项目，原 PM-SMALL 仅作为参考。没有本地原仓库时自行克隆只读参考副本，并从新应用的 Git 提交中排除参考副本。
2. 核对版本及公开数据接入能力，完成整体设计和规则对应表。表中每条写明旧来源、新模块、由框架承担还是应用补充，以及验证方式；不遗漏 UI、自动赎回和服务器运行。
3. 将整体设计写入 `docs/MIGRATION.md`，随后按设计继续编码，不等待我逐模块批准。技术选择自行作出；只有会改变已确定交易规则且无法从资料判断的问题才需要询问。
4. 先完整接通策略、UI、TEST、LIVE 接入代码及赎回流程，再进行整体验证。开发期间只运行能发现当前改动错误的针对性检查，不每改一个小问题都重新跑全套长期活动。
5. 用同一组行情、配置、时间和资金输入验证共同规则，重点检查 Event 仲裁、预算冻结、分批目标、30 秒止损、暂停、重启、结算及回款。对照的是规则和预期决策，不要求模拟和实盘获得相同成交收益。
6. 进行无真实资金的端到端测试：真实公开行情＋模拟成交，以及可控的订单/账户/赎回响应，覆盖部分成交、无成交、迟到回报和到账后继续运行。使用模拟响应的部分明确标记，不能宣称已经完成真实链上验证。
7. 检查前端关键交互、构建、持久化恢复及资金数量关系，再在 Linux 容器环境验证部署依赖。账户余额、框架持仓和应用周期必须能够相互核对；赎回前应收款不能提前成为现金。
8. 小问题集中修复并运行相关回归，整体完成后进行一次完整回归。影响交易规则、资金、持仓或测试证据有效性的问题立即处理。正式长期 TEST 在全部功能设计和交付完成后另行启动，不自动沿用旧项目的活动编号或累计时长。

不要因为钱包凭据、GitHub 登录或 SSH 暂时缺失，就停止本地实现。先完成所有不依赖这些条件的工作，最后集中列出实际缺项。

## 八、GitHub 与服务器交付

### GitHub

新应用建议命名为 `PM-NAUTILUS`，作为我的独立应用仓库；原 `yvettemiranda/PM-SMALL` 保留作参考。NautilusTrader 作为固定版本依赖，保留实际使用代码所需的许可证声明。

在本地完成 Git 初始化、合理分段的提交、依赖锁定和 CI 文件。CI 覆盖必要的规则检查、构建及 Linux 运行兼容性，不触碰真实钱包。

建议远端为 `yvettemiranda/PM-NAUTILUS`，默认私有；这是拟定目标，首次实际上传前核对账户和目标。若我已明确指定目标并要求上传，且登录可用，完成提交和推送，不重复请求同一授权。若只有本文件而尚未确认远端或登录，先完成本地交付及可执行的推送步骤，再一次性告知缺项。已有同名仓库时先检查内容，不覆盖或强推。

提交范围只含代码、锁文件、测试和无密钥的文档/配置样例。`.env`、私钥、访问令牌、实际钱包配置、数据库、交易历史、日志和备份不进入 Git。上传后核对远端完整 SHA 和 CI 结果；未上传时明确说明。

### 自有服务器

准备 Dockerfile、Compose、无密钥环境变量样例、持久化目录约定、健康检查、启动/停止/更新/回滚命令及 `docs/DEPLOY.md`。以实际 Linux 架构为准，不能把 Mac 构建的原生依赖直接复制过去。

现有项目文档提到 Docker、Nginx、HTTPS 和本机 3000 端口。这是历史参考，不代表已核对当前服务器。服务器地址、SSH 用户和运行目录以我在电脑端提供的部署目标为准，不从公开网页推断 SSH 身份。

缺少目标或 SSH 时，先交付可运行的部署文件和完整步骤。收到具体服务器部署要求且访问可用后，继续执行到 TEST 部署完成。实施前只读检查当前服务和目录，使用新应用目录和全新数据目录，保留旧程序与数据；不运行旧项目的重置流程来为新程序腾位置。

需要切换原有公网入口时，在部署方案中明确切换内容、旧服务备份及回滚办法；执行已获授权的切换，避免覆盖无关服务。若我仅要求准备部署，做到可审查、可执行的状态后再集中列出最终需要确认的目标和切换动作。

应用端口绑定本机，由 Nginx 提供 HTTPS 和访问认证。对改变设置、启动暂停等请求采用与实盘控制相符的最小身份及请求来源校验，不扩展成复杂权限系统。

服务器必须运行 GitHub 对应的同一代码提交，核对版本、容器、API、网页、持久化和同库重启。交付停在 `TEST + PAUSED`，LIVE 未启用。正式 TEST 启动和实盘启用不由部署动作自动触发。

## 九、持续工作记录与完成标准

长任务不能只依靠聊天历史。维护：

- `docs/MIGRATION.md`：采用版本、规则对应表、整体设计、必要差异及其依据。
- `HANDOFF.md`：已完成、正在做、实际阻塞、已运行命令及下一步，保留本次明确范围。
- `docs/VALIDATION.md`：验证结果、修正的原问题、模拟覆盖与真实环境尚未验证的部分。
- `docs/DEPLOY.md`：Mac 开发、GitHub 上传、Linux 部署、回滚、TEST 操作和后续 LIVE 配置方法。

中断或压缩上下文后先读这些文件、Git 状态和最近提交，再继续工作，不重新设计已经确定的规则。进度说明简短，明确实际完成和剩余工作。

完成交付时至少达到：

1. 新程序实际运行在 NautilusTrader 上，主要业务操作经过同一套策略规则。
2. 现有 UI 能操作新程序，所有设置真正生效，TEST 具有完整自动交易和结算回款流程。
3. LIVE 行情/交易接入、账户余额、订单回报、恢复及自动赎回代码已接通。不能以只含 Fake、Disabled、TODO 的骨架声称实盘功能完成；真实凭据配置和真实交易验收单独列明。
4. 不引入旧仓位和旧账本，不接管手动仓位，默认不运行实盘。
5. 关键规则验证、整体回归和 Linux 部署验证已有真实结果；未执行或受环境阻塞的项目如实标记。
6. 本地交付、GitHub 推送、服务器部署分别报告真实状态和版本。外部访问未提供时，仍交付完整代码和可执行步骤。

每次代码交付点简要说明改动及影响，并提供本次新增或修改文件从头到尾的完整代码；不要用省略号、局部 diff 或伪代码代替。文件较多时按文件分批呈现，保持代码可直接使用。只修改与本次迁移有关的内容。

现在开始检查工作区、读取原项目、核对 NautilusTrader 版本，写出完整迁移设计，然后继续实现和验证。
